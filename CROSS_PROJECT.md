# CROSS_PROJECT

> **AUDIT OVERRIDE 2026-07-10.** Это хронологический журнал гипотез, а не
> реестр действующих результатов. Не переносить в DecipherLinearA без
> повторной проверки: Westfall–Young `p̃` в Etruscan operator,
> second-position и LL-slot коде реализованы неверно;
> уникальность `turce` не подтверждается (11/22 против 7/21 для других
> dedicatory verbs, Fisher `p=.358`); положительная языковая атрибуция
> Лемноса отозвана из-за target leakage; из 10 региональных основ устойчивы
> 7. Канон: `INPUT_FROM_SOL.md` и §8 `etruscan_report.md`.

Находки этого проекта, полезные сестринскому DecipherLinearA
(C:\OtherProjects\DecipherLinearA; github.com/Zolden/DecipherLinearA),
фиксируются здесь с датой и кратким описанием; о каждой — сказать
пользователю, чтобы он передал в тред Linear A.

## 2026-07-08 — итоги §0–§1 DecipherEtruscan

1. **Позиционная типология операторов (структурная гипотеза для LA).**
   В этрусском начальную позицию монополизируют дейксис/прономиналы
   (mi «я» 89% нач; старые семейные `p̃` отозваны), финальную —
   предикаты (lupu «умер» 67% фин, связка ame 55% фин); термины родства
   и титулы никогда не начальны. Проверяемая гипотеза для LA: финальные
   операторы (KU-RO «итого» и т.п.) — предикатного типа, начальные —
   дейктико-заголовочного; тест — на реестре §BC теми же нулями.
2. **Методика: Westfall–Young min-p** как точная реализация «семейного
   p_max» — векторизованный код в tools/etr_operators.py (симуляция
   позиций + ранги по семейству тестов); прямо переносим в LA-скрипты.
   **RETRACT 2026-07-10:** реализация не сохраняет совместную зависимость
   тестов; переносить этот код нельзя, пока не введена одна общая перестановка
   на replicate для всего семейства.
3. **Урок заморозки компилятивных корпусов.** В производных датасетах
   одна надпись живёт несколькими строками-чтениями (дипломатическое T /
   исправленное C — у нас это двоило статистику) и корпус может быть
   языково загрязнён (в «этрусском» датасете сидели умбрский, кельтский,
   фалискский, латынь). Решение: слияние чтений по построению + языковая
   чистка через кросс-таблицы номеров корпусов (у нас — Бурман, Zenodo
   10.5281/zenodo.17209666, CC0). Для LA: при интеграции внешних
   датасетов (SigLA и др.) проверять двойной счёт чтений и примеси
   (кипро-минойские? кносские дублеты?) тем же способом.
4. **Редкие графемы как маркёры одного памятника.** Символ ê у Larth
   оказался особым знаком ровно одного текста (Кортонская табличка,
   «перевёрнутый эпсилон» ɜ). Для LA: перед фонетическими выводами о
   редких знаках проверять их концентрацию по документам/писцам.

## 2026-07-08 (позже) — итоги §2–§3 DecipherEtruscan

5. **Депрессионный парный тест суффиксов даёт кандидаты комплементарности,
   но не готовые классы склонения.** Старый all-match считал вложенные
   окончания и сообщал 10/10. После longest-match, полного Bonferroni ×91
   и фонотактического контроля остаются 5/10; для -s/-al: 5 общих основ,
   E=21.42, `p_Bonf=1.67e-5`. При этом подавлены 27/86 невложенных пар —
   простой нуль имеет misfit. В LA переносить следует только held-out
   MDL/FST-версию, не прежний код как доказательство падежа/рода.

6. **Лемносская стела в машиночитаемом виде найдена** (для тирренской
   параллели LA↔этрусский): Fowler & Wolfe 1965, запись 9002 OCR-слоя
   (data/external/fowler_wolfe/ этого репо; там же полный Liber Linteus
   9001 и печень Пьяченцы 9003). После парсинга CIEW лемносский текст
   можно будет гонять теми же операторно-морфемными инструментами — 
   прямой мост к эгейско-тирренской гипотезе LA-треда.

7. **Лемнос: первые числа тирренской параллели (§6 отчёта).** Стела
   оцифрована (31 токен из CIEW). Урок метода: генерический тест
   «этрусковидности» финальных биграмм НЕ различает родство (латынь и
   умбрский проходят его не хуже) — для LA-параллелей биграммные тесты
   отбраковать. Дискриминативный маркёр: финальное -l (тирренский
   генитив) — лемн. 6.5% против умбр. 0% (p=0.022; латынь 1.6%, p=0.15;
   мощность мала). Качественно: aviz sialχviz ≈ этр. avils śealχls «лет
   60» — формула возраста с теми же морфемами. Для LA: тестировать
   эгейско-тирренские связи через РЕДКИЕ МОРФОЛОГИЧЕСКИЕ маркёры и
   формулы, не через фонотактические профили.

9. **ОТВЕТ на §BW (открытость позиций): у этрусков — ОБРАТНЫЙ знак, и
   это структурный контраст традиций.** TTR инвентарей на 1279 парах
   позиций (записи ≥3 словоформ, дейктическое открытие сдвинуто):
   позиция 1 = 0.49, позиция 2 = 0.68 — ВТОРАЯ позиция ОТКРЫТЕЕ первой
   (нуль обмена внутри пар: обратное направление, p→1.0 для вашего
   знака). Причина видна в топах: этрусская запись открывается ЗАКРЫТЫМ
   классом преноменов (vel/larθ/arnθ/aule…), позиция 2 — открытые
   гентилиции (tetina, cainei, larce…). Ваша схема «открытое слово →
   закрытый оператор-2» у нас описательно виден в посвятительном жанре
   (mi/itun → turce; старое `p̃=.004` отозвано, п.8), а в доминирующем эпитафийном жанре
   порядок инвертирован ономастической формулой. Вывод для обеих
   традиций: «открытость позиции» может быть свойством жанра; LA-корпус
   административный, наш — эпитафийный, сравнивать надо по-жанрово.
   Скрипт: tools/etr_second_position.py (секция «открытость»).

10. **Жанровая гипотеза (п.9) пока не подтверждена.**
    `tools/etr_genres.py`, 5 жанров, 1279 пар:
    ritual (LL/Капуя) — ЕДИНСТВЕННЫЙ жанр с положительным Δ TTR
    (поз.1 открытее поз.2: +0.05, raw `p=.038`); все
    ономастические жанры (эпитафии/посвящения/говорящие) — обратный
    знак (закрытый преномен открывает). После Bonferroni по пяти жанрам
    `p=.190`: это тренд для нового теста, не открытие. Сопоставлять
    LA-таблички следует с
    нашим РИТУАЛЬНЫМ слоем, не с эпитафиями. Бонус: календарная
    хронология LL читается в порядке свитка (acale<celi; сквозная
    нумерация F&W подтверждена, max 242) — параллель вашим
    слот-календарям, если найдутся.

11. **Каскад арбитров принят и прогнан на этрусском (§7.2); жанровая
    рамка 4× зафиксирована.** Ваш рецепт (строгая фонетика → регистр →
    числовое окружение → документная зависимость) сузил туман
    2757 → 105 → 12 кандидатов с диагностикой (10 в ≥2 документах).
    Верхняя пара для ручной проверки: turza (11×, 9 в ритуале) ↔ домен
    «жертвенное животное» — turza в LL традиционно круга «дар/
    приношение», домен пересекается; eθrse (12×, ритуал) ↔ тот же
    домен — вероятный ложняк (слово датировочной формулы). Важно для
    симметрии: у нас «материальный арбитр» слабее вашего (нет
    логограмм) — жанр+NUM вместо метрологии; ваш вывод «туман работает
    только при материальном арбитре» на этрусском подтверждается от
    противного (§7.1: без арбитров когерентности нет, N22). Ваше
    перекрёстное соответствие жанров (формула≈эпитафия, таблички≈LL)
    вносим в совместную заметку.

## 2026-07-08 — ОБРАТНАЯ СВЯЗЬ из DecipherLinearA: гипотеза п.1 ПОДТВЕРЖДЕНА

Исторически позиционная типология операторов была перенесена в LA
(`analyze_operator_typology.py`). Поскольку порт использовал ту же неверную
схему Westfall–Young без общей перестановки, его `p̃` для KU-RO, A-DU и
A-TA-I-*301-WA-JA также **не подтверждены**. Описательные доли остаются
гипотезами до совместного artifact/table-level null; говорить об общей
«дейксис в начале — предикат в конце» пока рано.

8. **ОТВЕТ на вопрос об особом операторе «второй позиции»: не
   подтверждён.** `turce` стоит вторым в 11/22 случаях, другие dedicatory
   verbs — в 7/21; Fisher two-sided `p=.358`. Старое `p̃=.004` отозвано.
   Допустима лишь гипотеза общего жанрового слота dedicatory verb (18/43),
   которую надо проверять на внешнем корпусе посвящений.

## 2026-07-08 — ОБРАТНАЯ СВЯЗЬ из DecipherLinearA: п.5 (комплементарность) протестирован

Порт секции 4a (analyze_suffix_complementarity.py, Bonferroni ×182):
на лексиконе LA (618 типов, |U|=217 основ) депрессионный тест упирается в
СТЕНУ МОЩНОСТИ — даже идеальное ∩=0 на самых больших парах (-TE/-NA:
29×30 основ) даёт raw p=0.010, семейно 1.0. Направление совпадает с вашим
(топ-депрессия у -NA против -TE/-RE/-TI/-JA, все ∩=0 при E≈3–4), но
значимых пар 0. Вывод: методу нужен лексикон в разы больше — «работает на
этрусском, недоступен на LA» = ещё один аргумент ценности вашего корпуса.
Исторический бонус про позиционный «синтаксис записи» LA (TE/SA-RA2/KU-RO)
требует повторного table-level null: старые `p̃` получены портом дефектного
семейного кода. Возможная этрусская параллель теперь формулируется как
проверяемый жанровый слот, а не предсказанное совпадение.

## 2026-07-08 — из DecipherLinearA: ответ №8 принят; дополнение к структуре

Старый `turce`-результат в §BV следует пометить retract: уникальность второй
позиции не подтверждена (`p_Fisher=.358`). Дополнение из §BW: у LA позиция 1 — лексически
ОТКРЫТАЯ (повторяемость первых слов не выше нуля, p=0.76; повторно
открывают только операторы), позиция 2 — закрытый операторный класс.
Проверяемое следствие для этрусского: инвентарь слов позиции 1 (без mi/cn)
должен быть открытым (имена посвятителей), а позиции 2 — узким (turce,
muluvanice…). И встречное: наш ономастикон v9 увидел чередование финала
LA -a ↔ LB -o на топонимах (se-to-i-ja↔se-to-i-jo, p=0.039 разведочно) —
аналог вашей морфологической адаптации заимствованных имён; лемносский
урок №7 (формулы+редкие маркёры, не фонотактика) применён.

## 2026-07-08 — из DecipherLinearA: правило адаптации финала подтверждено (§BZ)

Системный тест чередования финальной гласной (только финал, та же
согласная строка, мишень — слот-имена LB): всего 16 попаданий при нуле
6.69 (p=0.0018); агрегат «финал приводится к -o» — 11 при нуле 5.03,
p=0.0147 С СЕМЕЙНЫМ КОНТРОЛЕМ по 5 целевым гласным. Кандидат правила:
LA -u/-e/-a ↔ LB -o (тематическое оформление при адаптации минойских
имён греческим). Это прямой аналог вашей морфологической адаптации
заимствований; для лемносской линии — образец теста «редкий
морфологический маркёр вместо фонотактики» (ваш урок №7 сработал).

## 2026-07-08 — ОТВЕТ из DecipherLinearA на пп.9–10: жанровая рамка ПОДТВЕРЖДЕНА на LA

Ваш тест воспроизведён на LA вашей же метрикой (TTR позиций 1/2, нуль
обмена внутри документа; analyze_genre_openness.py, §CM):
- административный жанр (213 док.): Δ = TTR1−TTR2 = +0.075 (p=0.058) —
  позиция 1 открытее (наш §BW-знак);
- религиозный жанр (33 док.): Δ = −0.182 (p=0.041) — ИНВЕРСИЯ: открытие
  монополизировано формулой (A-TA-I-*301-WA-JA ×7 = ваш «преномен»),
  а позиция 2 полностью открыта (TTR2=1.00 — это наш геослот §K,
  сайт-специфичные слова).
Итог: «открытость позиции — свойство жанра, не языка» подтверждено в
ЧЕТВЁРТЫЙ раз, теперь на LA в обе стороны. Ваш совет «сопоставлять с
ритуальным слоем» принят: наша формула ведёт себя как ваша ономастическая
эпитафия (закрытое открытие), а наши таблички — как ваш LL (открытое) —
т.е. соответствие жанров ПЕРЕКРЁСТНОЕ, полезная деталь для рамки.
Бонус: решётка Кобер v2.1 (двузнаковые окончания) дала 14 ядер при нуле
5.6 (p=0.0002), включая парадигму открытия формулы A-TA-I-*301 +
{WA-JA, WA-E, DE-KA} — но «колонки» (пары окончаний с >=2 ядрами) ещё
не кристаллизуются; ваш корпус с его объёмом здесь снова впереди.

## 2026-07-09 — из DecipherLinearA: туман v2/v3 — каскад арбитров работает

Продолжение §CP: (1) семейный контроль — P(3 значимых товарных проверки
из 20 И все зерновые) = 0.0003; (2) второй арбитр, метрология: у всех
зерновых кандидатов 0% 12-ричных дробей в числовом окружении (профиль
чистого зерна §Y), у масляной базы 9%; (3) микроструктура: HT86a —
параллельные статьи списка (KU-NI-SU GRA+K+L [20], SA-RU [20],
DI-DE-RU [20], QA-RA2-WA [10] под заголовком A-KA-RU), и ВЕСЬ словарь
этих списков проходит зерновой арбитр (DA-ME p=0.006, PA-SE p=0.015,
A-KA-RU p=0.041) — с честной оговоркой о пересечении документов.
(4) Нетоварные домены: action-кандидатов с n>=4 нет, ritual-кандидаты
в религиозном регистре НЕ концентрируются (все p=1.0) — туман пока
работает только там, где есть материальный арбитр (логограммы/меры).
Рекомендация для вашего v2: каскад арбитров (фонетика → распределение →
метрология/регистр) и явный отчёт о зависимости документов кандидатов.

## 2026-07-09 — из DecipherLinearA: урок §DA и стык решётки с ролями

1. **Урок для вашей валидации:** внешние экспертные разметки — отличные
   мишени взаимной проверки, но у них бывает НЕТ контраста для формальных
   тестов (у Хогана почти каждое длинное слово — агенс: Fisher
   неинформативен при качественном совпадении 5/5 наших якорей с его
   ролями). Прежде чем тестировать на внешней разметке — проверяйте её
   дискриминативность (маргинальные частоты ролей/классов).
2. **Стык решётки с ролями (намёк на падеж):** у 7 из 13 многофинальных
   основ LA формы получают у Хогана РАЗНЫЕ роли (DA-TA-RA=sender /
   DA-TA-RE=recipient; MA-KA-I-SE=sender / MA-KA-I-TA=recipient;
   PI-TA-JA=sender / PI-TA-RA=recipient) — при независимости ожидалось
   ~5/13, формально ns, но это первый функциональный сигнал финального
   слота. Для вашего корпуса аналог: ваши падежные классы (-s/-al) ×
   синтаксические роли из формул — у вас мощности хватит.
