# Supplement-механизм

Замороженный `data/etr_corpus.pkl` НЕ редактируется. Любые добавления к
корпусу — только через CSV-файлы в этой папке: `tools/etr_freeze.py`
подхватывает их автоматически (в алфавитном порядке имён) и включает в
пересборку с `src = SUPP:<имя файла>`; после добавления заморозка
перегоняется, новый sha256 фиксируется в `data/etr_corpus.sha256`,
изменение коммитится вместе с логом.

## Схема CSV (кодировка UTF-8, заголовок обязателен)

| колонка      | обяз. | содержимое |
|--------------|-------|------------|
| `text`       | да    | транслитерация (конвенции ETP-части: θ χ φ ś σ, разметка Лейдена, `|` — граница строки) |
| `source_id`  | нет   | внешний ID (сигла ET/ETP/CIE, издание) |
| `city`       | нет   | город/регион |
| `date_from`  | нет   | датировка, год до н.э. (число, ранняя граница) |
| `date_to`    | нет   | датировка, год до н.э. (поздняя граница) |
| `translation`| нет   | перевод (английский, как в основном корпусе) |
| `language`   | нет   | `etr` (по умолчанию) / `lat` / `umb` / … |
| `provenance` | да    | откуда взят текст: издание, страница, DOI/URL; загрузчик отвергает строку с непустым `text` и пустым provenance |
| `note`       | нет   | примечания |

## Уже добавлено и оставшиеся пробелы

1. **Скрижали Пирги** уже добавлены как `pyrgi_wikipedia.csv` (две
   этрусские пластины). Не добавлять вторую копию; следующий шаг — сверка
   чтения по критическому изданию и точный Wikipedia revision `oldid`.
2. **Liber Linteus** представлен 224 строками CIEW:9001 из OCR-извлечения
   Fowler & Wolfe, 49 строками CIEP:15910 и 12 ETP LL-записями. Это один
   памятник, а не независимые источники. Пробел — ручное выравнивание и
   сверка всего текста с современным критическим изданием.
3. **Tabula Capuana** представлена 74 строками CIEW:7002 и 23 строками
   CIEP:8682. Пробел тот же: критическая построчная сверка и модель
   альтернативных чтений, а не повторный ingest.
